var code;
const colorPool = ["red", "yellow", "orange", "green", "blue", "purple"];

window.onload = function() {
    generateCode();
}

function generateCode() {
    var colorsLeft = colorPool;
    for (let i = 0; i < 5; i++) {
        let index = Math.floor(Math.random() * colorPool.length);
        
    }
}